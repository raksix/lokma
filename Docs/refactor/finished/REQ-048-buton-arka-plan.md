# REQ-048 — Ayarlardaki Add Provider/Save butonlarına tema arka plan rengi

- **Status:** done (2026-09-08 — implemented + live verified, close-out commit below)
- **Asked:** 2026-09-08 — "ayarlarda falan add provider, save gibi bazı butonların arka plan rengi yok, onlar component olması lazımdı zaten, onlara temalarda arka plan rengi eklersin" (SS'e bakılamadı — görüntü servisi 500).
- **Interpretation:** Ayarlar yüzeyindeki (Providers, Settings, vb.) birincil aksiyon butonları (`Add Provider`, `Save`...) şeffaf/sınırsız görünüyor. Hepsi temalı `Button` componentine bağlanır (`variant="default"` → `--primary` bg), light/dark iki temada da dolgulu görünür. Tek tek class yaması yerine component standardı uygulanır.
- **Touched (plan):** ayar panellerindeki çıplak `<button>`lar → `Button` component + tema token kontrolü.
- **Touched (this run):** `packages/lokma-web/web/src/components/ui/button.tsx` only — all five variants + focus ring rewired from dead shadcn tokens (`bg-primary/bg-secondary/bg-accent/border-input/bg-background/ring-ring`, none present in `@theme`, compiled to nothing → transparent buttons) to existing `@theme` tokens: default `bg-terracotta text-white hover:bg-terracotta-hover`, secondary `bg-muted text-ink hover:bg-muted-2`, outline `border-line bg-paper`, ghost `hover:bg-muted`, destructive `bg-red-600`, ring `ring-terracotta`. No bare primary `<button>` remained in settings surfaces (all Save/Add already `Button`); Retry underline text-buttons intentionally untouched. Dark covered by existing `.dark .bg-terracotta/.bg-muted/.border-line/.text-ink` overrides. Out of scope (left for later REQs): `badge.tsx`/`input.tsx`/`app-shell.tsx` still reference dead `bg-primary/bg-background` tokens.
- **Proof:** root `bun x tsc --noEmit` 0 · web `bun run build` green (`index-BRYHgeAK.js` + `index-CSLTfspB.css`) · single-proc `lokma-web` restart (restarts 56→57, online) · served bundle == disk (`BUNDLE-MATCH /assets/index-BRYHgeAK.js`) · live CSS contains `.bg-terracotta` rule, zero `.bg-primary` (dead class gone from output).
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, headless SS (light+dark) ile dolgulu buton kanıtı, bundle match.
