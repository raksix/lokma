# REQ-027 — Bot sistemi refaktörü (Grok mantığı + Hermes bots referanslı)

- **Status:** done 2026-09-07 (implemented + live-verified, see Proof)
- **Asked:** 2026-09-07 — "abi bot sistemini refaktör edelim. grok bot çalışma mantığı. hermes bots diye bir şey oluşturu, hermes agentin reposundan adamlar nasıl yapmış bakabilirsin. ayrı sohbet arayüzleri falan var, sağlam bir şekilde o mantıkta yaparsın".
- **Interpretation:** Bot sistemi Grok tarzı çalışma mantığıyla elden geçirilir: her botun ayrı sohbet arayüzü, kendi kişiliği/belleği/bütçesiyle bağımsız çalışması. Referanslar: (1) Hermes agent reposundaki `bots` yapısı — uygulamada `hermes-agent` skill'i üzerinden incelenir (nasıl tanımlanmış, nasıl koşuyor, arayüzü nasıl); (2) `Docs/35-BOTS-lokma-bots.md` mevcut lokma bot kontratı. Hedef: sağlam, referanslı, ayrı-chat'li bot mimarisi.
- **Touched:** `lokma-core` session meta/summary `botId` (`session/types.ts`, `session/store.ts` — writeMeta merge + fork carry) + NEW `bots/chat-context.ts` (`resolveBotChatContext`, jailed `readBotKnowledge`, pure preamble/cap/join + NEW `chat-context.test.ts`) + `bots/store.ts` (`runBotAsAgent` stamps `botId`) + `bots/index.ts` (barrel); server `agent-loop.ts` (`systemPreamble`) + `routes/sessions.ts` (POST/PATCH/GET `botId`, 404 `bot_not_found`) + `routes/ws.ts` (per-turn model + SOUL/knowledge injection, deleted bot degrades to plain chat); web NEW `components/bots/bot-chat.ts` (pure switch/clear/name/filter + NEW `bot-chat.test.ts`) + `bots-pane.tsx` (Chat button opens fresh bot-bound session) + `chat/index.tsx` (header bot picker: search + Plain-chat + per-bot model) + `lib/api.ts` + `stores/session.ts` (`setSessionBot`); `Docs/35-BOTS-lokma-bots.md` NEW §8 Bot-Bound Sessions (old §8-10 renumbered §9-11).
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, headless ile bot seçip ayrı sohbette konuşma + bütçe/kişilik ayrımı kanıtlanır, bundle match.
- **Proof:** `chat-context.test.ts` 7/7 + `bot-chat.test.ts` 4/4, root `bun x tsc --noEmit` 0 errors, shared+core+ai+server builds 0, web vite build green `index-DtTHp5Em.js`, single-proc `lokma-server` + `lokma-web` restarts online, live health 200 + served `/assets/index-DtTHp5Em.js` == disk `dist/index.html` (BUNDLE-MATCH). Commit: refactor(web): REQ-027 Grok-style bot-bound separate chats (see git log)
