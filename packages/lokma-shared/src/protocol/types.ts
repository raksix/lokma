/**
 * Shared protocol type helpers — re-exported for convenience.
 * Keep WS framing in ws.ts, shared domain types here if needed in future.
 */
export type SessionId = string;
export type AgentId = string;
