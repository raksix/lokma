# REQ-026 — Uygulamadaki her şey pane olarak kullanılabilir olacak

- **Status:** done 2026-09-07 (implemented + live-verified, see Proof)
- **Asked:** 2026-09-07 — "en sol en sağdaki menüdeki şeyler de pane'e sürüklenirse pane olarak da kullanılabilir. uygulamadaki her şey pane olarak kullanılabilir yani öyle olacak".
- **Interpretation:** Sol/sağ şeritlerdeki TÜM ikonlar (Inspector menüleri, sessions, git, explorer öğeleri...) tiling workspace'e sürüklenebilir olur; bırakınca ilgili yüzey pane tab'ı olarak açılır (mevcut session/file drop akışıyla aynı desen: open / split / focus). Hedef: uygulamadaki her yüzey pane'e taşınabilir — istisnasız.
- **Touched:** `components/panes/panes.ts` (NEW `INSPECTOR_DRAG_MIME` + `RailDropId` (`InspectorTabId | 'sessions'`) + `isRailDropId`/`encodeInspectorDrag`/`parseInspectorDrop` pure helpers, MIME-only parse — label text fallback yok), `components/panes/pane.tsx` (`handleDrop` genelleşti: rail branch session'dan sonra file'dan ÖNCE — "Terminal" gibi label'lar rel-path sanılmasın; tool id → `makeInspectorTab` + center addTab / edge split, 'sessions' → pane picker açılır; `hasPanePayload` + toast + picker tip güncellendi), `components/shell/inspector-rail.tsx` (23 ikon draggable + drag payload + tooltip hint), `components/shell/activity-bar.tsx` (NEW pure `activityDragId(key)` — mapped tab ya da sessions için 'sessions'; 9 ikon draggable), `components/panes/index.ts` (barrel export). Session satırları (REQ-005) ve dosya satırları zaten sürüklenebilirdi — bu iş rail'lerdeki boşluğu kapattı.
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, headless ile her ikon tipinin drop ile pane tab'ı açtığı kanıtlanır, bundle match.
- **Proof:** `panes.test.ts` 87/87 (+11 rail checks), `activity-bar.test.ts` 17/17 (+3), `inspector-rail.test.ts` 9/9 (+2), root `bun x tsc --noEmit` 0 errors, vite build green `index-CJ4eVJd6.js`, `pm2 start ecosystem.config.cjs --only lokma-web` online (restart 34), served `/assets/index-CJ4eVJd6.js` == disk (BUNDLE-MATCH) + live bundle carries `application/x-lokma-inspector` + drag hint strings. Commit: refactor(web): REQ-026 every rail icon drops as a pane tab (see git log)
