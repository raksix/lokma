# lokma-ai

Multi-provider abstraction for [Lokma](https://github.com/raksix/lokma) — streaming chat over Anthropic and OpenAI-compatible APIs, plus the model catalog.

Part of lokma (`lokma-shared` + `lokma-core` + `lokma`). MIT licensed.
