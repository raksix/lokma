# REQ-089 — Pane fullscreen: header butonu + modal + split

- **Status:** done (2026-09-09)
- **Asked:** 2026-09-09 — "bu panenin buraya bir tuş ekle, ona basınca tam ekran yapsın modal olarak o paneyi; yukarda x ile kapatsın büyük ekranı. fullscreen browser değil, sadece o pane tam ekran olsun. tam ekran panellerde de split bölme falan olsun." (Ekran görüntüsü ekti; vision servisi 500 döndüğü için görüntü okunamadı — hedef pane kullanıcının tarifine göre Projects paneleri.)
- **Interpretation:** Her pane header'ına (önce Projects) fullscreen butonu: basınca SADECE o pane uygulama-içi modal olarak tam ekran açılır (browser fullscreen API DEĞİL), modalın üstünde X ile kapanır. Fullscreen modal içindeki pane'de split/bölme aksiyonları çalışmaya devam eder (yeni split'ler modal içinde açılır). Kapanınca pane eski yerine aynen döner (state kaybı yok). REQ-046 popout'tan farklı: ayrı pencere değil, uygulama-içi modal. 'yap' denmeden kod YOK.
- **Touched:** `panes/panes.ts` (pure `findLayoutNode` + `findParentNode` + `isPaneUnder`, probe'lu) + `panes/fullscreen-modal.tsx` (YENİ: `PaneFullscreenModal` fixed overlay `z-[70]` + header X/Esc, `FullscreenPlaceholder` arka-plan slotu; `bg-white` solid + `bg-black/60` overlay — `bg-background`/`bg-card` token yok) + `panes/pane.tsx` (strip `Maximize` butonu + sağ-tık "Open fullscreen", lucide only) + `panes/workspace.tsx` (`fullscreen {root,origin}` state, modal=canlı subtree view portal-to-body, split/splitEmpty/moveTab-edge auto-widen, collapse-narrow/close effect, reset temizler) + `panes/index.ts` (barrel) + `panes.test.ts` (18 yeni assert)
- **Proof:** panes probe 129/129 (089: 18/18) · root `bun x tsc --noEmit` 0 · web build green (`index-HerGtS4K.js`, `Open fullscreen` 3 + `Exit fullscreen` 3 + `Open in fullscreen` 1 chunk'ta) · concept build green · `pm2 restart lokma-web` online, served == disk (BUNDLE-MATCH) · headless E2E REQ089-E2E-PASS (3 pane/3 buton → modal 1 canlı pane + 1 placeholder → modal-içi split 2 pane → X kapatır, layout 3→4 canlı kalır, placeholder gider) · gate 401 ON (tokenless `/api/auth/me`)
- **Verify:** canlıda pane header'daki büyüteç tuşu → modal tam ekran + X/Esc kapatır; modal içinde split açılır (yan yana + alt alta + drop); kapatınca layout/state aynen geri gelir (split'ler canlı kalır).
