# REQ-032 — Models Refresh tüm provider'ların /v1/models'ine istek atsın

- **Status:** done 2026-09-07 (refactor-scan loop; live 42 models / 35 enabled)
- **Asked:** 2026-09-07 — "modellerde refresh edince otomatik tüm modellerin v1/models'ine istek atsın işte".
- **Interpretation:** Models panelindeki Refresh butonu SADECE önbelleği okumak yerine, aktif (key set) TÜM provider'ların canlı `/v1/models` endpoint'ine istek atar, dönen listeyi birleşik kataloğa yazar (`GET /api/models` + store tazelenir), başarısız provider'lar satırında hata rozetiyle işaretlenir ama diğerlerini bloklamaz. REQ-030/031 ile aynı akışın parçası.
- **Fix:** yeni `POST /api/models/refresh` — static catalog cache'ini düşürür, tüm enabled provider'lara paralel probe atar (6s bound, aynı `probeAllEnabled` helper — `GET /api/models` merge ile DRY), birleşik kataloğu + per-provider `providers[]` (`ok/modelCount/latencyMs/error/skipped`) döner. Client `refreshModelsLive()` kataloğu direkt store'a yazar, probe sonuçlarını `testResults`'a işler (skipped=keyless rozet dışı) — hatalı provider Providers satırında kırmızı rozet alır, diğerleri bloklanmaz. Models Refresh butonu busy spinner + özet toast (`Refreshed N models · M providers live · K skipped` / `F failed: ids`).
- **Touched:** `packages/lokma-web/server/src/routes/models.ts` (probeAllEnabled + mergeLiveIds extract, getMergedCatalog same behavior, POST route), `web/src/lib/api.ts` (ModelsRefresh types + refreshModels), `web/src/stores/provider.ts` (refreshModelsLive), `web/src/components/providers/models-pane.tsx` (live refresh + toast), `web/src/stores/stores.test.ts` (6 yeni assert).
- **Proof:** root+web `tsc` 0, stores probe ALL PASS, server+web build green, single-proc lokma-server + lokma-web restarts online, live `POST /api/models/refresh` 42 models / 35 enabled (opencode-go OK, 5 keyless skipped, ollama honest fail non-blocking), served bundle == disk dist (BUNDLE-MATCH, index-CWMUNsk7.js carries /api/models/refresh).
