# REQ-097 — Chat input'a yapıştırma (Ctrl+V) + sürükle-bırak dosya eki

- **Status:** done (canlıda — 2026-09-10)
- **Asked:** 2026-09-09 — "chat input'a ctrl+v ile resim/dosya yapıştırma ekle" + "drag drop da ekle".
- **Teşhis (koddan):** `composer.tsx`'te ek altyapısı VAR: `attachFiles` (text-only, 100KB cap) + `<attachment>` inline + explorer'dan drop `@path` olarak ekleniyor (`dropSignal`). EKSİK: (1) `onPaste` — panodaki dosya/resmi aynı hatta sokma; (2) OS'tan composera dosya sürükle-bırak (`onDrop` + `dataTransfer`); (3) resim hikayesi — bugün binary reddediliyor, resimler için vision-destekli ek (küçük önizleme + model vision) 'yap'ta kapsam kararı. 'yap' denmeden kod YOK.
- **Fix:** textarea `onPaste` clipboard dosyalarını aynı `attachFiles` hattına sokar (metin yapıştırma aynen akar); composer kökünde `Files` tipli OS sürüklemeleri için `onDragEnter/Over/Leave/Drop` + terracotta dashed overlay (`Drop files to attach`) — explorer `@path` drag'leri `Files` taşımadığı için parent Card hattı aynen durur, çift işlem yok. Resimler (mime ya da .png/.jpg/.jpeg/.gif/.webp) thumbnail çipli `image` eki olur, gönderide boyut+ölçülü `<attachment>` marker gider (kapsam kararı: model-vision bayt akışı follow-up, sunucuda vision kanalı yok); metin dosyaları eski 100KB inline hattında, resimlerde 5MB cap + toast. Ek varken boş metinle gönderim açıldı (`canSend`/`handleSend`/`deliver`), çiplerde önizleme + revoke'lu kaldırma, dosya seçicide resim uzantıları.
- **Touched:** `packages/lokma-web/web/src/components/chat/composer.tsx` (paste/drop/overlay/image-attach/send-with-attachments) + `composer-utils.ts` (`isImageAttachment`/`hasOsFiles`/`formatImageMarker`, saf) + `chat.test.ts` (8 yeni assert).
- **Verify:** chat.test 24/24 PASS; web `tsc --noEmit` 0; web build green index-DNSjYuUj.js (yeni `Drop files to attach` + `dimensions unknown` yalnız güncel bundle'da); `pm2 restart lokma-web` online, served == disk (BUNDLE-MATCH), chunk bayt-gerçek JS (SPA fallback değil), gate 401 ON.
