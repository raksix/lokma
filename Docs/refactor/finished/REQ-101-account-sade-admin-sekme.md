# REQ-101 — Account yalnız kendi bilgisi; admin'e ayrı sekme

- **Status:** done (canlıda — 2026-09-10)
- **Asked:** 2026-09-10 — "ayarlarda accountta sadece kendi hesap bilgilerim gözüksün. Admin özellikleri için ayarlarda ayrı sekme falan açarsın."
- **Teşhis (koddan):** Settings → Account bugün `LazyAuthPane`'i render ediyor (`settings-modal.tsx:193-194`) — kendi profili + kullanıcı tablosu + rol/proje/member yönetimi hepsi aynı ekranda. İstek: Account = yalnız kendi hesap bilgileri; kullanıcı/rol/proje atama gibi admin işleri `SETTINGS_SECTIONS`'a eklenecek ayrı sekmeye (adı 'yap'ta: Users/Admin) taşınır + sekme navigasyonda yetkiye göre gizlenir (`auth:manage` yoksa DOM'da yok) + içerik route-guard'lı. `SECTION_ICONS`'a ikon + `settings.ts` probuna yeni id eklenir.
- **Fix:** `auth-pane.tsx` → `git mv` ile `admin-pane.tsx` (`AdminPane`: rol kartları + superadmin policy + Projects + Users/invite + flow notu; imzasız/kurucusuz/non-admin'e `AdminLockedNote`, sign-in/out formları SİLİNDİ) + yeni `account-pane.tsx` (`AccountPane`: yalnız kendi profil kartı — avatar/ad/e-posta/rol/durum/son aktiflik + Sign out; imzasızken sign-in/invite/seed formları, id'ler korundu). `SETTINGS_SECTIONS`'a `{id:'admin',label:'Admin'}` (account'tan sonra) + `SECTION_ICONS`'a `ShieldCheck`; modal her açılışta `authMeQuiet` ile `me` çeker — Admin sekmesi `canDo(me,'manageUsers')` (admin+superadmin, server `requireAdmin` ile aynı küme) yoksa nav'da DOM'a çıkmaz + içerik guard'lı + deep-link `initialSection='admin'` yetkisizde Account'a bounce eder. Inspector `auth` sekmesi → `LazyAdminPane` (kendi-gated). `LazyAuthPane`/`AuthPane` silindi (kalan referans yok).
- **Touched:** `auth/admin-pane.tsx` (rename+trim, −195 satır form), `auth/account-pane.tsx` (yeni), `auth/index.ts`, `panes/lazy-panes.tsx`, `panes/inspector-host.tsx`, `settings/settings.ts`, `settings/settings-modal.tsx`, `settings/settings-modal.test.ts` (13 section + admin asserts), `app-shell.tsx` yorumu, `lib/api.ts` yorumu, `shell/narrow-layout.test.ts` (allow-list yolu `account-pane.tsx`'e).
- **Verify:** web `tsc --noEmit` 0 + root `tsc` 0; settings-modal probe 17/17; auth probe 68/68; narrow-layout 5 fail PRE-EXISTING (browser/terminal/onboarding dosyaları, benim dosyalarımda 0 fail); web build green (`index-CPut4PVf.js`, `admin-pane-BGfNxeE3.js` + `account-pane-CKa4iEFA.js` chunk'ları index'ten referanslı); `pm2 restart lokma-web` online; served == disk (BUNDLE-MATCH); chunk'lar byte-gerçek JS; gate 401 ON (tokenless `/api/auth/me` + `/api/users` 401, superadmin token ile ikisi de 200).
- **Not (bilinçli):** Projects (visibility/member) bölümü Admin'e taşındı — calisan-owner'lar üye yönetimini artık göremez (Teşhis'teki "proje atama taşınır" kapsamı; server yetkileri değişmedi). Sekme etiketi `Admin` seçildi (`Users` değil — policy+projects de içinde).
