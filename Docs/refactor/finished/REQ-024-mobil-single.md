# REQ-024 — Mobil görünüm ayrı sade deneyim: pane sistemi yok, single view

- **Status:** done 2026-09-07 (implemented + live-verified, see commit hash note at bottom)
- **Asked:** 2026-09-07 — "mobil görünüm için ayrı bir kısım gibi olacak, her özellik olacak ama pane sistemine gerek yok, mobilde sadece single görünüm falan olsun".
- **Interpretation:** Mobil breakpoint altında uygulama ayrı sade moda geçer: TÜM özellikler erişilebilir kalır (chat, sessions, explorer, inspector içerikleri, ayarlar) ama tiling/windowed pane sistemi yoktur — sadece single görünüm (tek yüzey + alt navigasyon sekmeleri). Mevcut mobil drawer'lar bu yapıya evrilir; pane sürükle-bırak, split, windowed mobilde hiç açılmaz.
- **Touched:** `components/shell/responsive.ts` (`MobileTab`/`MOBILE_TABS`/`mobileTabLabel`/`isPaneSystemAllowed`/`isMobileViewport`), `components/shell/mobile-single-view.tsx` (NEW: bottom tab bar Chat/Sessions/Files/Tools + scrollable 23-tool pill picker + single surface), `components/shell/mobile-single-view.test.ts` (NEW probe 29/29), `components/shell/index.ts` (barrel exports), `components/app-shell.tsx` (mobile-only branch rendering `MobileSingleView`, force `setTiling(false)`+`setWindowed(false)` on mobile, `TilingToggle` hidden on mobile, header `hideSideToggles`), `components/header.tsx` (optional `hideSideToggles` hides swap + panel toggles, search/settings/theme stay), `components/files/file-browser.tsx` (openFile skips tiling + pane-tab request on mobile, inline preview/editor is the surface), `components/sessions/sessions-sidebar.tsx` (rows not draggable + "open as pane" hidden on mobile, drag/open handlers no-op via `isMobileViewport`). All literal Tailwind classes, lucide only, quoted TS keys.
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, 390px headless ile tüm özelliklere ulaşıldığı + pane UI'ının hiç render olmadığı kanıtlanır, bundle match.
- **Proof:** root + web `tsc --noEmit` 0 errors, `mobile-single-view.test.ts` 29/29 + `responsive.test.ts` 41/41, vite build green `index-BroxmYo3.js`, `pm2 start ecosystem.config.cjs --only lokma-web` (online), served `/assets/index-BroxmYo3.js` == disk (BUNDLE-MATCH), 390px headless: mobile nav with Chat/Sessions/Files/Tools present, tiling toggle + both rails absent, single-view rendered, 0 JS errors. Pre-existing `narrow-layout.test.ts` failures (config-pane grids, header toolbar, browser-pane allowlist) verified identical on clean HEAD — zero new violations from this change. Commit: refactor(web): REQ-024 mobile single-view (see git log)
