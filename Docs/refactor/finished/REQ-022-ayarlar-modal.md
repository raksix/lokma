# REQ-022 — Ayarlar ekranda modal olarak açılsın

- **Status:** done 2026-09-07 (implemented + live-verified, see commit hash note at bottom)
- **Asked:** 2026-09-07 — "abi ayarlar modal olarak açılsın ekranda, ekran görüntüsündeki gibi" (+ ekran görüntüsü: OpenCode tarzı büyük ayarlar modalı).
- **Interpretation:** Ayarlar (REQ-009'daki detaylı sistem) pane/sekme yerine ekran ortasında büyük MODAL olarak açılır: arkası karartılmış backdrop, solda kategori navigasyonu + sağda içerik, sağ üstte X, Escape ile kapanır, focus trap'li. Referans bölümler (OpenCode): Model, Chat, Appearance, Workspace, Safety, Browser, Memory & Context, Voice, Advanced, Notifications, Billing, Providers, Gateways, Keyboard Shortcuts, Tools & Keys, Plugins, Archived Chats, About — lokma karşılıklarıyla (Providers, Models, Appearance, Sessions, Permissions, MCP, Memory/Vault, Cron, Shortcuts, Plugins, About...). Dişli ikonları (REQ-012 üst bar + REQ-008 activity bar) bu modalı açar.
- **Deviation (documented):** the REQ suggested the `?` shortcut, but `?` already opens the shortcuts help dialog (SHORTCUTS registry, a11y-probe gated) — stealing it would break help. Settings instead opens on the standard `Ctrl/⌘+,` combo, registered in SHORTCUTS so the `?` dialog lists it. The Inspector `settings` tab itself stays mounted for deep-links (Extras board, tiling host); all three gears route to the modal.
- **Touched:** new `components/settings/settings-modal.tsx` (dialog shell + left nav + right content; Config/Appearance/Permissions/MCP share one GET /api/config load, Providers/Models/Memory/Cron/Plugins embed the existing lazy panes, Shortcuts renders the SHORTCUTS registry, About shows live server version), `settings.ts` (+SETTINGS_SECTIONS registry, isSettingsSection, DEFAULT_SETTINGS_SECTION), `settings-modal.test.ts` probe (13/13), `settings/index.ts` barrel, `shell/shortcuts.ts` (+settings entry), `app-shell.tsx` (settingsOpen state, header/activity/rail wiring, Ctrl+, handler, lazy modal chunk).
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, headless ile modalın açılıp kategoriler arası geçiş + Escape/X ile kapanma + ayar kaydetme kanıtlanır, bundle match.
- **Proof:** settings-modal probe 13/13 + settings probe 92/92 + responsive 41/41 (a11y 35/36 — 1 pre-existing stale SHORTCUTS.map check, fails on clean HEAD too), web `tsc` 0, vite build green index-Cn79R3pz.js + settings-modal-BLrBpr1t.js chunk, `pm2 start ecosystem.config.cjs --only lokma-web`, served `/assets/index-Cn79R3pz.js` == disk (BUNDLE-MATCH). Headless probe 9/9 PASS (boots, no modal before open, header gear opens, 11 nav buttons, Providers/Shortcuts/About present, General default, Shortcuts switch shows registry, Providers loads live pane, Escape closes). Commit: refactor(web): REQ-022 settings as OpenCode-style modal (see git log)
