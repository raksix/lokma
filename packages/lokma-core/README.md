# lokma-core

Lokma harness kernel — agent loop, tools, sessions, providers, config, skills, memory. Powers the `lokma` CLI and the Web harness.

Part of [lokma](https://github.com/raksix/lokma) (`lokma-shared` + `lokma-ai`). MIT licensed.
