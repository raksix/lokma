# REQ-045 — Tiling üst menü çubuğu kaldırılsın

- **Status:** done (2026-09-08 — implemented + live verified, close-out commit below)
- **Asked:** 2026-09-08 — "tiling kısmından yukarıdaki bu menüyü kaldır, bence gerek yok, otomatik single ve tiling otomatik açılıyor zaten" (SS'e bakılamadı — görüntü servisi 500 — ama TilingBar koddan biliniyor).
- **Interpretation:** Tiling workspace'in üstündeki `TilingBar` şeridi (Tiling etiketi, sayaç, 20 araç butonu, + Pane, Windowed, Save, Reset, Single) tamamen kaldırılır — single/tiling geçişleri zaten otomatik olduğu için gereksiz. Araç açma rail ikonlarından, pane ekleme/bölme pane şeridinden yapılır.
- **Touched (plan):** `components/panes/workspace.tsx` (TilingBar render'ı), `tiling-bar.tsx` (ölü kalırsa silinir).
- **Touched (this run):** `panes/workspace.tsx` — `<TilingBar>` render + `openInspector`/`addPane`/`save` handlers + `tabCount` dropped; Reset survives as a `RESET_LAYOUT_EVENT` window listener (verbatim old body, same FOCUS_FILES_EVENT pattern). `panes/tiling-bar.tsx` → `panes/tab-icons.tsx` via `git mv` (TilingBar component deleted, shared `TAB_ICONS` kept for `pane.tsx`); `panes/index.ts` re-export updated. `panes/panes.ts` — new `RESET_LAYOUT_EVENT` const. `app-shell.tsx` — `TilingToggle` is now bidirectional: single view keeps the enter button, tiling shows a compact 3-button cluster (Single exit + Windowed toggle + Reset dispatch). Save (toast-only) and the pane/tab counters are gone with the strip. Deviation note: Single/Windowed/Reset had NO other surface (removing them would strand persisted `tiling:true`/`windowed:true` users with no exit), so they live on as the minimal cluster — the 20-button tool menu itself is fully gone.
- **Proof:** root `bun x tsc --noEmit` 0 · web `tsc -b && vite build` green (`index-B9yM1rKd.js`) · probes `panes` 96/96 + `stores` ALL PASS + `mobile-single-view` 29/29 (1 source-grep assertion updated to the new `if (isMobile) return null` shape, behavior unchanged) + `shell` 10/10 + `inspector-rail` 9/9 · single-proc `lokma-web` restart online, served bundle sha == disk · headless Chromium (basic-auth): tiling enter renders 3 panes with 0 strip buttons/titles/label, cluster Single+Windowed+Reset present, Single click exits back to chat.
- **Commit:** c50c69e
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, headless ile şeridin DOM'da olmadığı + geçişlerin çalıştığı kanıtlanır, bundle match.
