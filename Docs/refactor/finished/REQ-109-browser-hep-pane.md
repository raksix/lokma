# REQ-109 — Browser her seferinde pane olarak açılsın, sidebar'dan asla

- **Status:** done (canlıda — 2026-09-10, commit 9007b67)
- **Asked:** 2026-09-10 — "browser hiçbir şekilde sidebarlardan açılmamalı, direkt session gibi pane olarak açılmalı her seferinde!"
- **Teşhis (koddan):** browser iki yoldan açılıyordu: (1) pane sekmesi (`open_browser` ui_action + tiling browser tabı — doğru yol), (2) sidebar Inspector sekmesi (activity-bar `browser` key'i + rail `browser` girdisi → Inspector browser tabı — kalkan yol).
- **Touched:** `panes/panes.ts` (yeni `PANE_ONLY_TABS` + `isPaneOnlyTab`, pane registry/drag id'leri korunur) + `panes/index.ts` (barrel) + `shell/activity-bar.tsx` (yeni `activityOpensPaneTab`, click/drag ayrımı; drag id `browser` kalır) + `shell/index.ts` (barrel) + `app-shell.tsx` (yeni `openBrowserPane`: desktop'ta `setTiling(true)` + `requestInspectorTab('browser')`, mobilde drawer fallback; activity/rail/`open_browser` ui_action hepsi buraya bağlandı; `InspectorPanel`'e `browserMode`) + `providers/inspector-panel.tsx` (yeni `browserMode` prop: desktop `pane-redirect` kartı — canlı browser yok, tek tıkla pane açar; mobil `inline` gerçek browser) + `shell/mobile-single-view.tsx` (`browserMode="inline"`) + `panes.test.ts` (6 yeni assert) + `activity-bar.test.ts` (2 yeni assert).
- **Verify:** desktop'ta browser'a nereden basılırsa basılsın (activity ikonu, rail ikonu, agent `open_browser`, sidebar Extras Open, redirect kartı) pane sekmesi açılır (upsert + focus, tiling kapalıysa workspace açılır); desktop sidebar'da browser yüzeyi kalmaz (stale state redirect kartına düşer); per-agent sekmeler korunur (`browser-pane.tsx`'e dokunulmadı); mobil Tools Browser çalışmaya devam eder.
- **Proof:** panes 97/111/135 + activity-bar 19/19 + inspector-rail 9/9 + mobile-single-view 31/31, root tsc 0, web build green index-pmxAKnyd.js (yeni stringler current bundle'da), concept build green, pm2 restart lokma-web online, served index == disk (BUNDLE-MATCH), gate 401 ON.
