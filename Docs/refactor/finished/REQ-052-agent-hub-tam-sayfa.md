# REQ-052 — Agent Hub tam sayfa kaplasın, daha detaylı olsun

- **Status:** done 2026-09-08 (implemented + live)
- **Asked:** 2026-09-08 — "agent hub tam sayfayı kapasın, daha detaylı olsun" (SS'e bakılamadı — görüntü servisi 500).
- **Interpretation:** Agent Hub dar panel yerine tam sayfa görünüm olur: her agent için durum (running/idle/error), model, bütçe/kota çubuğu, son aktivite, tokEN harcaması + toplu aksiyonlar (pause/resume/kill). REQ-025 (tasarım iyileştirme) ile birleşir — bu istek sayfa kapsamını, REQ-025 görsel dili tanımlar.
- **Touched (plan):** agents/orchestration pane'leri (tam sayfa düzen + detay kartları).
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, headless SS (light+dark) kanıtı, bundle match.
- **Done (2026-09-08):** narrow 46/54 split replaced by a full-width responsive card grid (1/2/3 cols) with the detail section below it. Every card now shows status dot + name/id + ai/queue badges + persona/model/state + budget cap line + cap bar + created relative time + cwd. New Bulk bar (Pause all / Resume all / Kill all with eligible counts, same guards as the per-agent buttons via `bulkTargets`, sequential moves so one 409 never aborts the rest). Detail meta gained last-activity from the live trace timeline (`useAgentActivity`, newest trace event + relative time; renders nothing when the trace is empty/fails). Token SPEND stays hidden — no spend endpoint exists and the Hub never invents usage figures (cap bar keeps the honest 0% + cap label). Touched: web components/agents/agents-pane.tsx + agents.ts (`formatRelativeTime`, `bulkTargets`) + agents.test.ts (+12 checks). Gates: agents probe 54/54, web tsc 0, web build green agents-pane-DV3opTx-.js, single-proc lokma-web restart online, served index chunk == disk (MATCH), served agents chunk 200 carries Bulk/Pause-all/last labels, /health 200.
