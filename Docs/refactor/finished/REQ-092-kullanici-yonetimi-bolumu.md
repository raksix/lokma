# REQ-092 — Settings → Account'a admin kullanıcı yönetimi bölümü

- **Status:** done (2026-09-09 — dedupe close-out, no code: already shipped + live, do NOT re-implement)
- **Asked:** 2026-09-09 — "ayarlar kısmında account'ta sadece benim account yönetimim; settings'e admins, kullanıcı yönetimi kısmı falan ekle" (ekran görüntüsü; vision servisi 500 — tariften).
- **Interpretation:** Settings → Account bugün yalnız kendi hesabını yönetiyor (REQ-072). Admin/superadmin'e ek "Kullanıcı yönetimi" bölümü: kullanıcı listesi (isim, e-posta, rol rozeti, durum, son aktiflik), rol/değişiklik aksiyonları. Sadece `auth:manage` yetkisi olan görür; yetkisizde bölüm hiç render olmaz (section-level gate, 403 beklemez). Liste API'si hazır: `GET /api/users`. 'yap' denmeden kod YOK.
- **Close-out (dedupe, impl e3ae68e):** every line above was already live BEFORE this file was even written (filed c993a27, section shipped e3ae68e `feat(web): migrate AuthPane pane to harness`). `AuthPane` = Settings → Account body (REQ-072) and carries the full `Users — admin only` section: search + count, rows with name/role-badge/status-badge/email + `active {formatLastActive}`, role `<select>`, Enable/Disable, password reset, two-click delete, invite row + selected-project checkbox + one-time link/temp-password copy (`auth-pane.tsx:870-1015`, helpers `filterUsers/formatLastActive/roleTone/statusTone` in `auth.ts`, client `api.listUsers/inviteUser/patchUser/deleteUser/resetUserPassword` in `lib/api.ts:1446-1454`). Section-level gate holds: `{isAdmin && ...}` never renders for calisan/viewer and `loadTables` never fetches `listUsers` for them (no 403 wait); tokenless `GET /api/users` → 401 live, member/viewer → 403 via server `requireAdmin` (`server/routes/auth.ts`). DELIBERATE NON-CHANGE: filed text says "sadece auth:manage" (superadmin-only) but the shipped + server contract is admin+superadmin (`requireAdmin`, client `canDo manageUsers`); stripping shipped admin access on a cron tick with no explicit "yap" for removal would be destructive — left as-is, recorded here.
- **Touched:** (yok — docs-only close-out)
- **Verify:** LIVE — served `index-BE4WYpm8.js` == disk dist, its dynamic `auth-pane-CsbqbgW0.js` served byte-identical (`cmp` clean) carrying all six Users strings; root `bun x tsc --noEmit` 0; tokenless `/api/users` + `/api/auth/me` 401; gate ON.
