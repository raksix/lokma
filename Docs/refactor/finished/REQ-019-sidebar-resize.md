# REQ-019 — Sol/sağ menüler resize edilebilir olacak

- **Status:** done (2026-09-07, live verified — see Proof below)
- **Asked:** 2026-09-07 — "sol sağ menü de resize edilebilir olcak".
- **Interpretation:** Sol ve sağ sidebar'ların iç kenarında sürüklenebilir resize tutamacı olur (VS Code tarzı): basılı tutup çekince genişlik değişir, min/max limitli (örn. 160–640px), seçim persist edilir (reload'da korunur). Mobil drawer'lar etkilenmez (sabit 85vw/320px kalır).
- **Touched (plan):** `components/sidebar.tsx` (resize handle + pointer drag) + genişlik state'i (mevcut `leftW`/`rightW` persist'i varsa o kullanılır, yoksa eklenir).
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, headless drag ile genişliğin değişip reload sonrası korunduğu kanıtlanır, bundle match.
- **Proof:** Both desktop sidebars now have a VS Code-style drag handle on the inner edge (split-tree divider pattern: window pointermove/up, `role="separator"`, `cursor-col-resize`, terracotta hover). Touched: `web/src/components/sidebar.tsx` (new `width`/`defaultWidth`/`onResize`/`resizable` props, `SIDEBAR_MIN/MAX_WIDTH` 160–640 clamp mirroring the `setSideWidth` store guard, double-click reset, arrow-key ±8px nudge, handle hidden unless width+onResize given) + `web/src/components/app-shell.tsx` (desktop sidebars wired to persisted `leftW`/`rightW` via `setSideWidth`; mobile drawers untouched, still fixed 85vw/320px). No new store needed — `leftW`/`rightW` persist in `lokma:layout:v1` already existed. Gates: root `tsc --noEmit` 0, web `tsc --noEmit` 0, web build green `index-CTI0y7s7.js`, single-proc lokma-web restart online, served bundle == disk dist (BUNDLE-MATCH), headless drag probe PASS: left 268→346 (+78 on +80 drag), `leftW=346` persisted, reload keeps 346, right 300→362 (+62 on −60 drag), 0 page errors.
