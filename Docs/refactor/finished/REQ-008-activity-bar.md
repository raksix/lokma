# REQ-008 — En sağa VS Code tarzı ince activity bar ekle

- **Status:** done (live 2026-09-07)
- **Asked:** 2026-09-07 — "en sağda bir tane panel ekle vscodedaki gibi orda işte burda olması gerekenleri oraya ekle... en üstte sessions onun altında git... en sağa küçük bir sidebar sabit kalacak" (+ ekran görüntüsü: VS Code activity bar referansı).
- **Interpretation:** App'in en sağına VS Code activity bar benzeri ince (48px), sabit bir ikon şeridi eklendi. Yukarıdan aşağı: sessions (en üstte), altında git, altında pane kısayolları (terminal, browser, vault, testing, bots), en altta settings (dişli) + account. İkona tıklayınca ilgili panel açılır: sessions Explorer tarafını gösterir, diğerleri Inspector tarafını gösterip ilgili sekmeyi seçer (settings → Settings sekmesi, account → Auth sekmesi). Yeni veri katmanı yok — mevcut Inspector sekmeleri yeniden kullanılır. Mobilde gizli (`hidden md:flex`), drawer UX korunur.
- **Touched:** `components/shell/activity-bar.tsx` (yeni: `ActivityKey`, `ACTIVITY_ITEMS`, `activityInspectorTab` mapper, `ActivityBar`; lucide-only, terracotta aktif durum, `border-line` ayraçlar), `components/shell/activity-bar.test.ts` (yeni: 14 mapper/table probu), `components/shell/index.ts` (barrel), `components/providers/inspector-panel.tsx` (`InspectorTab` export + `requestedTab` prop — prop-driven olduğu için kapalıyken gelen ilk tıklama da doğru sekmeye düşer), `components/providers/index.ts` (tip re-export), `components/app-shell.tsx` (`activity`/`inspectorTab` state + `handleActivitySelect` + rail render en sağda). Ayrıca önceki yarım kalmış işten kalan yanlış ağaçtaki (`concept/`, ölü prototip) untracked `ActivityBar.tsx` silindi — implementasyon canlı ağaçta (`packages/lokma-web/web`).
- **Proof:** activity-bar probe 14/14, root `tsc --noEmit` 0, web `bun run build` green (`index-C4P5muUk.js`), single-proc `lokma-web` restart, live `/` 401 anon / 200 authed + `/health` 200, served bundle == disk dist, rail kodu canlı pakette (`Activity bar` + `Testing Lab` index chunk'ta).
