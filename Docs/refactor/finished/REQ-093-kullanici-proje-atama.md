# REQ-093 — Kullanıcı ekleme + projeye atama + proje-açma yetkisi

- **Status:** done (canlıda — 2026-09-09)
- **Asked:** 2026-09-09 — "ordan kullanıcı ekleyip, kullanıcıya istediğim kadar proje ekleyebileyim ya da proje açma yetkisi de ekleyebileyim".
- **Interpretation:** REQ-092 ekranından uçtan uca: (1) kullanıcı ekleme — mevcut invite akışı (`POST /api/users/invite` + `/invite` kayıt) modal/sayfa olarak buraya taşınır; (2) projeye atama — bir kullanıcıya istenen kadar proje (`POST /api/projects/:id/members`, çoklu seçim + listeden kaldırma); (3) proje-açma yetkisi — rol yükseltme (calisan→admin) veya proje-bazlı `project:create` grant'i (hangisi: 'yap'ta politika kararıyla, varsayılan rol yükseltme). API'lerin tamamı hazır, iş UI + akış. 'yap' denmeden kod YOK.
- **Fix:** `auth-pane.tsx` (Users — admin only): (1) davet formu artık tek "seçili projeye ekle" kutusu yerine TÜM projelerin çoklu checkbox listesi (`inviteForm.projectIds` doğrudan `POST /api/users/invite` gövdesine gider — API zaten dizi kabul ediyordu); (2) her kullanıcı satırında chevron (lucide `ChevronDown`) ile açılır proje paneli — `Promise.all(listMembers)` ile o kullanıcının üyelikleri listelenir, üye OLMADIĞI projeler select + Assign ile eklenir, her satırda Remove; (3) proje-açma yetkisi için kod YOK — politika kararı "rol yükseltme": satırdaki rol select'i (calisan→admin) + superadmin `projectCreation` politikası (`admin-only`/`members`/`open`) zaten canlıydı, dokunulmadı.
- **Touched:** `packages/lokma-web/web/src/components/auth/auth-pane.tsx` (import `ChevronDown` + `ProjectMember`; state `expandedUserId/userMemberships/userMembershipsBusy/assignProjectId`; handler'lar `loadUserMemberships/toggleUserProjects/assignUserToProject/unassignUserFromProject`; satır paneli JSX; davet çoklu-proje listesi).
- **Verify:** canlıda üyelik turu (superadmin token): listProjects 200 → listMembers 200 → create 200 → addMember 200 → removeMember 200 (liste boş) → deleteProject 200; scratch proje silindi, canlı state temiz. Web `del()` bodysiz (Fastify boş-JSON 400'üne takılmaz).
- **Proof:** root `bun x tsc --noEmit` 0; web build green (`auth-pane-B7V-eKXM.js` yeni string'leri taşır, eski "Add to selected project" 0); `pm2 restart lokma-web` online; served `index-DpYS-nc4.js` == disk, auth chunk byte-identical; gate ON (değişmedi).
