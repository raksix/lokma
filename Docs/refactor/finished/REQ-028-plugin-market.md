# REQ-028 — Skill market + plugin market + plugin sistemi (+ örnek plugin)

- **Status:** done 2026-09-07 (implemented + live-verified, see Proof)
- **Asked:** 2026-09-07 — "abi açık kaynak zaten, kendi için skill market, plugin market, plugin sistemi vardı, example plugin de yaz. uygulamanın her şeyine plugin yapılabilir olacak, o tür özellikler de eklersin".
- **Interpretation:** (1) Uygulama içi **skill market** (skill keşfet/kur/kaldır), (2) **plugin market** (plugin keşfet/kur/kaldır), (3) genelleştirilmiş **plugin sistemi**: uygulamanın HER şeyine (paneller, rail ikonları, komutlar, temalar, bot yetenekleri...) plugin yazılabilir — extension point + manifest + lifecycle (enable/disable) + sandbox/izolasyon notları. (4) En az bir **example plugin** (kurulum + hello-world pane + dokümantasyonuyla). Referanslar: `Docs/23-PLUGIN-SYSTEM-deepseek-cordis.md`, `Docs/27-SKILLS-auto-discovery-hermes-inspired.md`, mevcut 6 bundled `@lokma/plugin-*`.
- **Scope note (this run):** plugin market + kernel registry/hot-toggle/suspend-guard already shipped (Plugins pane, `lokma-core/src/plugins/`). This run closed the remaining gaps: **skill marketplace end-to-end** (search + git-clone install + Skills-pane Market tab) and the **example skill package** (`plugins/lokma-skill-hello/`, installable via the new installer, opens in the Skills pane via skill_view). NOT in this run: UI extension-point runtime (pane/rail/command/theme contributions) and `lokma skills install` CLI — follow-up REQs if the user wants them.
- **Touched:** NEW `lokma-core/src/skills/marketplace.ts` (`SKILL_MARKETPLACE_TOPIC`, `buildSkillMarketQuery`, `searchSkillMarketplace` reusing plugin parse/normalize helpers, `slugFromSkillUrl`, `installSkillFromUrl` = validate + `git clone --depth 1` + root-SKILL.md frontmatter check + rescan + rollback) + NEW `marketplace.test.ts` (28 asserts) + `skills/registry.ts` (`parseFrontmatter` exported, `linkedFiles()` helper shared by category + flat branches — flat/installed skills now keep their reference chips) + `skills/index.ts` (barrel); server `routes/skills.ts` (`GET /api/skills/marketplace`, `POST /api/skills/install`); web `lib/api.ts` (`SkillInstallRes`, `searchSkillMarketplace`, `installSkill`) + `skills-pane.tsx` (Registry/Marketplace header tabs, market search + Install rows + honest empty state, install reloads registry); NEW `plugins/lokma-skill-hello/` (`SKILL.md` + `references/hello-pane.md` + `README.md`, also copied to `~/.lokma/skills/` as the live demo install).
- **Proof:** core probe 28/28 PASS (live GitHub hit: topic `lokma-skill` = 0 repos, empty state honest); example indexes as `lokma-skill-hello` with `linked=["references/hello-pane.md"]`, 7 skills total; root `bun x tsc --noEmit` 0, web `tsc --noEmit` 0, core+server builds 0, web vite build green `index-C6o_YeD9.js`; single-proc `lokma-server` + `lokma-web` restarts online; live: `POST /api/skills/install` bad-protocol + empty → 400, `GET /api/skills` 200 (hello present), `GET /api/skills/marketplace` 200 `source=github-topic:lokma-skill` count 0, health 200, served bundle == disk dist (BUNDLE-MATCH); headless PASS (Skills opens, Marketplace tab renders + empty-state text, Registry lists hello, 0 pageerrors). Commit: refactor(web): REQ-028 skill marketplace + example skill (see git log)
