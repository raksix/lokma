# REQ-107 — Terminal SSH hissi versin

- **Status:** done (canlıda — 2026-09-10, commit 7b3ac29)
- **Asked:** 2026-09-10 — "bu terminal sanki ssh ile sunucuya bağlanmışım gibi olmalı."
- **Teşhis:** Terminal zaten gerçek PTY (REQ-059) + proje cwd (REQ-060) + tam-boy scrollback (REQ-079). Eksik his noktası: soket durumu pane'de görünmezdi — `ws.status` sadece aria-label'daydı; bağlantı düşünce yazı sessizce yutuluyor, sahte `$` imleç satırı ise canlıymış gibi nabız atmaya devam ediyordu. Ayrıca shell yaşayıp soket kopukken else-dalı yanlışlıkla "Shell ended" butonu gösteriyordu.
- **Touched:** `terminal/terminal.ts` (yeni saf `connectionNotice(status)` helper: open→null, idle/connecting→bekleme satırı, closed/error→reconnect aksiyonlu satır) + `terminal/terminal-pane.tsx` (scrollback-içi bildirim satırı: beklemede dönen LoaderCircle, kopukta `ws.reconnect()` çağıran amber Unplug butonu; sahte `$` satırı sadece `selectedRunning && wsLive` iken; "Shell ended" butonu sadece `!selectedRunning` iken — REQ-079 chrome-free korundu, header bar yok) + `terminal/terminal.test.ts` (6 yeni assert).
- **Verify:** kopuşta amber "Disconnected — click to reconnect" satırı, bağlanırken "Connecting to shell backend…" satırı, açıkken satır yok; tek tıkla `ws.reconnect()`; F5 sonrası shell tail-catch-up ile yaşar (değişmedi).
- **Proof:** terminal probe 56/56, web `tsc -b` 0, web build green index-D9Z1Wp0Q.js + terminal-pane-BXBrIYz8.js, pm2 restart lokma-web online, served index == disk + canlı chunk `Connecting to shell backend` taşıyor (BUNDLE-MATCH), gate 401 ON (tokenless /api/auth/me + /api/terminals 401). Not: tick ortasında kardeş REQ-108 işi ağacı kırdı (build red) — beklendi, kardeş close-out sonrası (4e7334b+b0c6889) yeşilde devam edildi; `git add` sadece bu dosyalara.
- **Proof-2 (prompt, 2026-09-10):** `shellCommand` artık `bash --login -i` (`--norc` atıldı) + HOME/USER/LOGNAME/LANG garantili → spawn tail `root@furkan-openclaw:/mnt/apopic/lokma# ` (çıplak `bash-5.2#` bitti); login shell'de tek-echo doğrulandı (`q`→1). Çift-yazma hattı uçtan uca tek ölçüldü (CDP framesent `["q"]` → tek echo); istemcide REQ-085/086 dedup + sunucu tek-write duruyor.
