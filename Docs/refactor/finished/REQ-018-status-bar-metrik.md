# REQ-018 — Status bar gerekli bilgileri versin

- **Status:** done (2026-09-07, live verified — see Proof below)
- **Asked:** 2026-09-07 — "status bar bunun gibi değil de daha gerekli bilgiler falan versin: işte gateway durumu, seçili proje, cpu kullanımı, ram kullanımı, token/s, lokma sürümü".
- **Interpretation:** Alttaki `FooterBar` ("All systems normal · [ / ] panels · Ctrl+K search · ? shortcuts / Lokma harness · Vite + Fastify + WS") yerine canlı bilgiler: gateway durumu (up/down + gecikme), seçili proje (aktif session'ın cwd/proje adı), CPU kullanımı %, RAM kullanımı (kullanılan/toplam), token/s (akış hızı, WS cost feed'den), lokma sürümü (package.json). Değerler periyodik tazelenir (mevcut 30sn health poll ritmi kullanılır, token/s WS'ten canlı). Kısayol ipuçları `?` penceresine taşınır.
- **Touched (plan):** `components/shell/footer-bar.tsx` (yeni alanlar + poll), server'da eksik metrik varsa endpoint (`/api/doctor` genişletilir ya da yeni `/api/metrics` — karar uygulamada).
- **Verify (plan):** root+web `tsc` 0, web build green, single-proc restart, headless ile her alanın gerçek değer gösterdiği (sahte "0%" yok) kanıtlanır, bundle match.
- **Proof:** FooterBar is now a live metrics strip — gateway reachability + round-trip ms (timed `/api/health`), active-session project (cwd basename from session store), host CPU % + RAM used/total from new `GET /api/metrics` (node:os cpus delta, null on first sample — never fake 0; version + uptime + process rss/heap included), stream tok/s from WS cost deltas (4s idle decay), lokma version; static panel/search hints removed (only the `?` shortcuts button stays). Touched: `server/src/routes/health.ts` (metricsRoutes inside healthRoutes, no new registration), `web/src/lib/api.ts` (MetricsRes + getMetrics), `web/src/components/shell/footer-bar.tsx` (rewrite, lucide Activity/FolderGit2/Cpu/MemoryStick/Zap/Tag), `web/src/components/app-shell.tsx` (30s tick times health + pulls metrics best-effort, cost-delta rate effect, projectName memo). Gates: root `tsc --noEmit` 0, server typecheck 0, web build green `index-CiRE5Ki5.js`, single-proc lokma-server + lokma-web restarts online, live `:3456/api/metrics` returns real numbers (cpu null first sample → 33.2%, mem 59.9/62.6 GB), served bundle == disk dist (BUNDLE-MATCH), headless footer text proves every field live: `gateway · 4ms / no project (stale local-only id honestly unmatched — server list has no such session) / cpu 30.3% / ram 59.9 GB / 62.6 GB / idle / ? / v0.0.1`, 0 probe errors.
